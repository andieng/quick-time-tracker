NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Server-only — never expose this to the browser. Used to rate-limit
# /api/feedback via a table the public API can't otherwise reach.
SUPABASE_SECRET_KEY=
